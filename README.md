1. 이름 : ROAD A EYE (고속도로 CCTV 기반 AI 위험 물체 감지 관제 시스템)

> MBC 아카데미 AI-X 3기 최종 프로젝트 4조

| 이름 | 역할 | 담당 |
|------|------|------|
| 노형래 | Leader · DB | DB 설계 · 이중화(HA) · 인프라 총괄 |
| 이지건 | AI (Keras) | 딥러닝 분류 모델 학습 · AI 서버 모델 연동 |
| 임효정 | AI (YOLO) | 객체 탐지 모델 학습 · 데이터셋 구축 |
| 김사브리나 | Front-end | Next.js 대시보드 33개 페이지 구현 |
| 심유경 | Back-end | FastAPI 8개 라우터 · 4개 DB 연동 |

------------------------------------------------------------------------------------------------------------------------------------------------

2. 링크
1) FastAPI 자동 문서 : http://localhost:8000/docs
2) 홈페이지 : http://localhost:3000

------------------------------------------------------------------------------------------------------------------------------------------------

3. 배경

국토교통부 통계에 따르면 고속도로 교통사고의 주요 원인 중 하나는 킥보드 · 오토바이 · 건설 차량 등 **통행 금지 차량의 진입**과 **역주행**입니다. 이러한 사고는 짧은 시간 안에 다중 추돌로 이어져 인명 피해가 크지만, 기존 관제 방식은 수십 개의 CCTV 화면을 관제 요원이 육안으로 모니터링하는 구조여서 즉각적인 대응에 한계가 있습니다.

이 과정에서 발생하는 구조적 문제는 크게 세 가지입니다.

**첫째, 육안 모니터링의 집중력 한계입니다.**
수십 개의 CCTV를 동시에 주시하는 것은 인지 부하 측면에서 근본적인 한계를 가집니다. 짧은 순간 화면을 놓치면 위험 상황을 인지하지 못한 채 지나칠 수 있습니다.

**둘째, 이상 상황 탐지의 지연입니다.**
위험 차량이 진입한 뒤 관제 요원이 이를 인지하고 대응 조치를 취하기까지 걸리는 시간이 사고 발생 여부를 결정짓는 핵심 변수입니다. 수동 모니터링 체계에서는 이 탐지 지연을 최소화하기 어렵습니다.

**셋째, 반복 패턴 데이터 부재입니다.**
특정 구간에서 위험 차량이 반복적으로 출현하더라도, 수기 기록 방식으로는 패턴을 통계적으로 분석하고 예방 조치를 취하는 것이 사실상 불가능합니다.

------------------------------------------------------------------------------------------------------------------------------------------------

4. 수요조사

1) 문제 인식 : 고속도로에서 반복적으로 발생하는 통행 금지 차량 사고를 예방하고, 실시간으로 신속하게 대응하는 안전한 교통 시스템에 대한 수요는 계속 늘고 있습니다.

2) 관련 언론 보도 사례
| 매체 | 보도일 | 내용 |
|------|--------|------|
| MBC 뉴스투데이 | 2026년 3월 17일 | 고속도로 오토바이 진입 사고 |
| SBS 뉴스 | 2025년 10월 31일 | 고속도로 전동킥보드 진입 사고 |
| 채널A | 2026년 2월 4일 | 전동킥보드 등 개인형 이동장치 위험 사례 |

3) 기존 관제 방식의 한계 : 수십 대의 CCTV를 관제 요원이 육안으로 순차 확인하는 현재 방식으로는 위험 물체 진입을 즉시 포착하기 어렵고, 확인 · 대응까지의 시간 지연이 2차 사고로 이어질 위험이 있습니다.

4) 핵심 니즈 요약 : 여러 매체에서 꾸준히 보도되는 진입 금지 차량 · 개인형 이동장치 사고 사례는, CCTV 영상을 실시간으로 분석해 위험 물체를 자동으로 감지하고 관제 요원에게 즉시 알리는 시스템에 대한 사회적 수요가 분명함을 보여줍니다. 본 프로젝트는 이러한 수요에서 출발했습니다.

------------------------------------------------------------------------------------------------------------------------------------------------

5. 목적 : CCTV 영상 → YOLO · Keras 기반 자동 감지 → 관제 대시보드 실시간 반영

------------------------------------------------------------------------------------------------------------------------------------------------

6. 목표

본 프로젝트는 위 문제를 해소하기 위해 실시간 CCTV 스트리밍, YOLO · Keras 앙상블 기반 위험 물체 자동 감지, 통계 · 히트맵 분석, AI 관제 어시스턴트를 결합한 통합 관제 플랫폼 구축을 목표로 합니다. 관제 요원의 육안 모니터링 의존도를 낮추고, 감지부터 알림 · 기록까지 이어지는 흐름을 자동화하는 것을 핵심 설계 원칙으로 삼았습니다.

1) 1차 목표 : CCTV 실시간 MJPEG 스트리밍 및 등록 · 관리
2) 2차 목표 : YOLO 기반 위험 물체 자동 감지 및 감지 기록 저장
3) 3차 목표 : 감지 통계 · 히트맵 · 반복 출현 패턴 분석
4) 4차 목표 : ITS API 연동을 통한 고속도로 CCTV 자동 동기화
5) 최종 목표 : AI 관제 어시스턴트 + 소셜 로그인 + 게시판 + 모델 버전 관리를 통합한 고속도로 CCTV AI 관제 플랫폼 구축
→ 실시간 스트리밍 + 위험 물체 자동 감지 + 감지 통계/히트맵 + AI 관제 어시스턴트 + DB 이중화 인프라

------------------------------------------------------------------------------------------------------------------------------------------------

7. 시스템 구조

```
관제 요원 (브라우저)
   ↓
Next.js 프론트엔드 (Front 서버)
   ↓
FastAPI 백엔드 (Back 서버) ── JWT 인증 · REST API · AI 프록시
   ↓                      ↓
MySQL DB (DB 백업 서버)   AI 서버 ── Keras 게이트 → YOLOv8s / YOLOv11m Soft Voting
(4개 DB, Master-Master 이중화)      ↓
                          고속도로 CCTV 스트림 (ITS API, RTSP/HTTP)
```

4개 서버로 역할을 분산했습니다. **AI 서버**는 YOLO · Keras 추론과 LLM 챗봇 프록시를, **백엔드 서버**는 REST API · JWT 인증 · AI 프록시를 담당하며, **프론트 서버**는 DB에 직접 접속하지 않고 백엔드 API만 호출합니다. **DB 백업 서버**는 4개 DB(member · board · ai · chat)의 복제본을 보관하며, Keepalived 기반 VIP로 장애 시 자동 페일오버됩니다.

------------------------------------------------------------------------------------------------------------------------------------------------

8. 전체 디렉토리 구조

```
Road-A-Eye/
├── apps/
│   ├── back/FastAPI/                    # 백엔드 (FastAPI)
│   │   ├── main.py                      # 앱 진입점 (lifespan, 라우터 등록)
│   │   ├── env.example                  # 환경변수 템플릿
│   │   ├── core/                        # config · database · security
│   │   ├── models/                      # SQLAlchemy ORM (User, CCTV, Detection, Board, Chat, Admin ...)
│   │   ├── routers/                     # auth · cctv · board · chat · model · ws · its · admin · settings
│   │   └── services/                    # 비즈니스 로직 (StreamManager, LLM 호출 등)
│   │
│   ├── ai/road-ai/                      # AI 추론 서버 (FastAPI)
│   │   ├── app/api/v1/                  # chat · its · keras · llm · yolo 엔드포인트
│   │   ├── app/modules/                 # keras(분류) · yolo(탐지) · llm(챗봇) · its(연동) 도메인 로직
│   │   ├── models/keras/                # 배포된 Keras 모델 버전 메타데이터
│   │   └── scripts/                     # validate_model.py 등
│   │
│   └── front/Project_HTML/project-html/ # 프론트엔드 (Next.js 16 · React 19)
│       ├── app/dashboard/               # 관제 대시보드 · 분석 센터 · 관리자
│       ├── app/board/                   # 공지 · 문의 · FAQ · 자료실 · 버그
│       ├── components/                  # 공통 UI · 대시보드 위젯
│       └── lib/                         # api client · 알림 · 스트림 훅
│
├── db/
│   ├── schema/                          # 4개 DB 스키마 DDL (member/board/ai/chat_db)
│   └── presentation/                    # 최종 발표 자료 (Next.js 프레젠테이션 앱)
│
└── docs/ssh/                            # 4개 서버 SSH 접속 참고 문서 (공개키 · 설정 예시만 포함)
```

------------------------------------------------------------------------------------------------------------------------------------------------

9. 백엔드 진행 순서

1) 프로젝트 기반 세팅
- FastAPI REST API 서버, SQLAlchemy 비동기 ORM(AsyncSession) 구성
- 라우터 · 서비스 · 스키마 · 모델 계층 분리
- Pydantic 스키마 유효성 검사, pydantic-settings 환경변수 관리

2) 인증 · 보안
- JWT 액세스 토큰 발급 · 검증, bcrypt 비밀번호 해싱
- 소셜 로그인 OAuth2 (네이버 · 카카오 · 구글)
- 이메일 인증 코드 발송 · 검증, 아이디 · 비밀번호 찾기
- 로그인 시도 횟수 제한(Brute Force 방지), 역할 기반 권한(user · admin)

3) 사용자 관리
- 회원가입 · 로그인 · 로그아웃, 소셜 계정 연동
- 개인정보 조회 · 수정, 비밀번호 변경 · 재설정, 회원 탈퇴

4) CCTV · AI 감지 연동
- CCTV 등록 · 관리, OpenCV 기반 RTSP/HTTP → MJPEG 스트리밍
- AI 서버로부터 위험 물체 감지 결과 수신 · 기록
- 감지 상태 관리(UNREAD · CONFIRMED · DISMISSED), 감지 통계 · 히트맵

5) 게시판
- 공지사항 · 1:1 문의(첨부파일) · FAQ · 자료실 CRUD

6) 관리자 기능
- 사용자 목록 · 검색 · 역할 변경 · 정지 · 삭제
- 활동 로그 기록 · 조회, 시스템 설정

7) 실시간 · 부가 기능
- WebSocket 실시간 알림, ITS 외부 API 연동
- FastAPI-Mail 이메일 발송, systemd 기반 서버 자동 기동

8) DB 이중화 · 인프라 (DB · 인프라 담당)
- 4개 DB(member · ai · board · chat) 도메인 분리 설계
- MySQL Master-Master 복제(binlog, auto_increment offset 분리)
- Keepalived VRRP VIP 기반 자동 페일오버 (헬스체크 2초 간격)
- 일일 자동 백업(cron, mysqldump --single-transaction, 7일 보관)

------------------------------------------------------------------------------------------------------------------------------------------------

10. 프론트엔드 진행 순서

1) 스택 세팅
- Next.js 16(App Router) · React 19 · TypeScript
- CSS Modules + CSS 변수 기반 다크 · 라이트 테마

2) 공통 UI
- 반응형 글로벌 헤더(권한별 메뉴 노출), 테마 토글
- 공통 모달 시스템, AI 챗봇 플로팅 어시스턴트

3) 인증
- 소셜 로그인(Google · Kakao · Naver) 및 콜백 처리
- 로그인 상태 실시간 반영

4) 실시간 관제 (관리자)
- 통합 관제 대시보드, 스트림 관리(전국 CCTV 검색 → AI 분석 시작 · 중지)
- AI 모델 비교(이미지 업로드로 여러 모델 결과 비교)

5) 분석 센터
- 통계 리포트(기간별 감지 추이 · 유형별 현황 · CCTV 순위)
- 위험 구간 지도(히트맵), 감지 기록 검토(확인 · 반려), AI 모델 관리

6) 관리자 시스템
- 사용자 관리(권한 변경 · 정지 · 삭제), 활동 로그, 정지 문의 처리

7) 게시판 · 기타
- 공지사항 · FAQ · 1:1 문의, 알림 이력, 환경설정, 마이페이지

------------------------------------------------------------------------------------------------------------------------------------------------

11. 기술 스택

1) 개발 환경
- Ubuntu 24.04 LTS, VSCode Remote SSH

2) 프론트
- Next.js 16(App Router), React 19, TypeScript
- CSS Modules, recharts(통계 시각화)

3) 백엔드
- FastAPI, Uvicorn, Python 3.11+
- SQLAlchemy 2.0(비동기 ORM) + aiomysql
- JWT(PyJWT), bcrypt
- OpenCV(headless) — RTSP → MJPEG 스트리밍

4) AI · 모델
- Keras(MobileNetV2 전이학습) — 1차 분류 게이트
- YOLOv8s / YOLOv11m(Ultralytics) — 위험 물체 탐지
- OpenAI API(gpt-4o) — AI 관제 어시스턴트, SSE 스트리밍

5) DB
- MySQL 8.0, Master-Master 복제 + Keepalived VIP 이중화

6) 인프라 · 연동 · 협업
- Keepalived(Failover), ITS API(고속도로 CCTV 연동)
- Git, Notion

------------------------------------------------------------------------------------------------------------------------------------------------

12. AI 탐지 대상

1) 위험 물체 감지 클래스 (총 10종)
전동킥보드 · 경운기 · 굴착기 · 리어카 · 전동휠체어 · 지게차 · 트랙터 · 오토바이 · 사람 · 차량

2) AI 관제 어시스턴트 분석 항목 (`services/chat_service.py`)
| 분석 대상 | 예시 질문 |
|-----------|-----------|
| 감지 현황 요약 | "오늘 감지된 위험 물체는 몇 건인가요?" |
| 위험 구간 분석 | "가장 위험 감지가 많은 CCTV 구간은 어디인가요?" |
| 반복 패턴 파악 | "이번 주 반복 출현 패턴이 있나요?" |
| 미처리 건 안내 | "처리되지 않은 감지 기록이 있나요?" |
| CCTV 운영 현황 | "현재 비활성 상태인 CCTV가 있나요?" |
| 시스템 상태 | "AI 모델 현재 버전은 무엇인가요?" |

3) 모델 개발 과정 및 성능
- 데이터셋 : 총 6,903장 (Train 4,470 · 64.8% / Validation 1,600 · 23.2% / Test 833 · 12.1%)
- Keras 분류 모델 : MobileNetV2 전이학습 기반, 1차 ~ 14차 반복 실험(optimizer · backbone · 해상도 비교) 끝에 최종 v14 모델 정확도 약 99% 달성. 1차 학습 기준 mAP50 97.6%
- YOLOv8s 탐지 모델 : 1차 학습 기준 Precision 0.930 / Recall 0.923 / mAP50 0.948 / mAP50-95 0.724
- YOLOv11m 탐지 모델 : 1 · 2 · 3차 반복 학습. 2차에서 imgsz · multi_scale 과도 적용으로 학습 시간 급증 및 성능 저하 → 3차에서 설정을 안정화(imgsz 640, lr0 0.001)해 개선. 최종 3차 mAP50 99.22%(1차 대비 +0.07%p), mAP50-95 84.30%, 학습 시간 750분 → 95분으로 단축
- 최종 앙상블 구조 : Keras가 1차 게이트로 금지 물체 가능성을 판단 → YOLOv8s · YOLOv11m 탐지 실행 → 두 결과를 Soft Voting으로 병합해 최종 신뢰도 산출. 연산량은 줄이고 탐지 정확도는 높이는 구조

4) 탐지 한계 및 보완 방식
- YOLO 신뢰도(confidence) 임계값 조정으로 오탐 · 미탐 균형 유지, 모델 버전 관리로 교체 이력 추적
- 네트워크 상태에 따른 RTSP 스트림 품질 저하 시 StreamManager가 자동 재연결 시도

------------------------------------------------------------------------------------------------------------------------------------------------

13. 주요 기능

1) 사용자 관리
- 회원가입 / 로그인 / JWT 인증 / 소셜 로그인(네이버 · 카카오 · 구글)
- 이메일 인증 기반 가입, 사용자 설정 · 프로필 관리

2) CCTV 관리
- CCTV 수동 등록 · 수정 · 삭제(비활성 처리)
- ITS API 자동 동기화, 활성 / 비활성 토글

3) 실시간 스트리밍
- RTSP · HTTP 스트림을 MJPEG으로 변환해 브라우저에 직접 전송
- CCTV별 OpenCV VideoCapture 캐싱 · 재사용, 단일 프레임 스냅샷 제공

4) 위험 물체 감지
- AI 서버가 감지 시 이미지 · 메타데이터를 백엔드로 전송해 기록 저장
- 감지 클래스 · 신뢰도 · 이미지 경로 · 상태 저장, 상태 관리(신규 → 확인 중 → 처리 완료)
- 금지 클래스 설정(킥보드 · 오토바이 · 건설 차량 · 역주행 차량 등)

5) 통계 및 분석
- 일별 감지 건수 추이, CCTV별 위험도 히트맵
- 반복 출현 패턴, 미확인 감지 건수

6) AI 관제 어시스턴트
- gpt-4o 기반 자연어 질의응답, 세션별 대화 히스토리 유지(최대 20개)
- SSE 스트리밍으로 토큰 단위 실시간 응답
- 감지 기록 · CCTV 현황 기반 상황 분석 및 조언

7) 게시판
- 공지사항, 1:1 문의(첨부 이미지), FAQ, 자료실, 버그 리포트

8) AI 모델 버전 관리
- 배포된 YOLO / Keras 모델 버전 이력 등록, 활성 모델 지정 및 메모 관리

------------------------------------------------------------------------------------------------------------------------------------------------

14. 실시간 처리 구조

본 시스템은 FastAPI(Python) + Next.js(TypeScript) 서버 구조로 구성되며, 클라이언트 - 서버 간 실시간 통신을 위해 MJPEG 스트림 · SSE · WebSocket을 용도별로 함께 사용합니다.

```
관제 요원 브라우저
  │
  ├─ HTTP (REST API)
  │    └─ fetch/axios → FastAPI 라우터 → SQLAlchemy → MySQL
  │
  ├─ MJPEG 스트림
  │    └─ <img src="/cctv/{id}/stream"> → StreamManager(OpenCV VideoCapture 캐싱)
  │
  ├─ SSE (AI 챗봇 스트리밍)
  │    └─ EventSource /chat/stream → chat_service → OpenAI API 토큰 단위 전달
  │
  └─ WebSocket (실시간 알림)
       └─ ws.py → 감지 이벤트 브로드캐스트
```

1) CCTV 목록 조회 : 클라이언트가 REST API를 호출하면 FastAPI가 MySQL에서 조회해 JSON으로 응답합니다.
2) 실시간 스트리밍 : `<img>` 태그가 MJPEG 스트림 엔드포인트를 직접 구독하며, `StreamManager`가 CCTV별 `VideoCapture`를 캐싱해 재사용합니다.
3) 위험 물체 감지 : AI 서버가 YOLO · Keras 추론 결과를 백엔드로 전송하면, 감지 기록이 저장되고 WebSocket으로 관제 화면에 즉시 알림됩니다.
4) AI 관제 어시스턴트 : 사용자 질문이 `/chat/stream`으로 전달되면 실시간 감지 · CCTV 데이터를 컨텍스트로 삽입해 OpenAI API가 답변을 생성하고, SSE로 글자 단위 스트리밍합니다.

------------------------------------------------------------------------------------------------------------------------------------------------

15. JWT 인증 흐름

1) 이메일 · 비밀번호 로그인
- 사용자가 이메일 · 비밀번호 입력 후 로그인 요청
- FastAPI가 DB에서 사용자 조회 → bcrypt 비밀번호 검증
- 검증 성공 시 JWT Access Token 발급
- 프론트엔드가 localStorage에 토큰 저장, 이후 요청마다 `Authorization: Bearer <JWT>` 자동 첨부
- `get_current_user()`가 토큰 검증 후 요청 처리

2) 소셜 로그인 (네이버 · 카카오 · 구글)
- 로그인 버튼 → OAuth 인가 URL 생성 → Provider로 리다이렉트
- Provider 인증 완료 → 콜백에서 Authorization Code를 Access Token으로 교환
- Provider로부터 사용자 정보 조회 → 신규 등록 또는 기존 계정 연동 → 자체 JWT 발급
- 프론트엔드 콜백 페이지로 리다이렉트 후 토큰 저장

3) 보안 설계 포인트
- JWT Payload에 `user_no`, `login_id`만 포함해 민감 정보 노출 최소화
- 이메일 인증 완료 후에만 로그인 허용
- 관리자 엔드포인트에 `require_admin` 의존성으로 권한 분리
- 로그인 시도 횟수 제한으로 Brute Force 방지

------------------------------------------------------------------------------------------------------------------------------------------------

16. 프론트 화면 구성

Next.js 16 App Router 기반으로 구성되었으며, 전 페이지 다크 · 라이트 테마 전환을 지원합니다. 총 33개 페이지, 9개 공통 컴포넌트로 구현되었습니다.

1) 화면 구성
- 로그인 / 회원가입 : 이메일 · 소셜 로그인
- 통합 관제 대시보드 : 실시간 CCTV 뷰, 오늘의 감지 건수 · 활성 CCTV · AI 정확도
- 스트림 관리 : 전국 CCTV 검색 → AI 분석 시작 · 중지
- AI 모델 비교 : 이미지 업로드로 여러 모델 결과 비교
- 분석 센터 : 통계 리포트, 위험 구간 히트맵, 감지 기록 검토, AI 모델 관리
- 관리자 시스템 : 사용자 관리, 활동 로그, 정지 문의 처리
- 게시판 : 공지사항 · FAQ · 1:1 문의 · 자료실 · 버그 리포트
- 마이페이지 : 알림 이력 · 환경설정 · 프로필

2) UI 설계 원칙
- 다크 · 라이트 테마 전환 시 깜빡임 방지
- 공통 모달 시스템으로 alert · confirm 일괄 교체(22개 페이지 적용)
- 권한 기반 메뉴 노출(비회원 · 일반 회원 · 관리자)

------------------------------------------------------------------------------------------------------------------------------------------------

17. 기대 효과

1) 위험 탐지 자동화 : YOLO · Keras 앙상블이 CCTV 영상을 실시간으로 분석해 위험 물체를 즉시 감지합니다. 관제 요원의 육안 모니터링 부담을 최소화합니다.
2) 대응 시간 단축 : 감지 즉시 대시보드 알림 및 WebSocket Push로 관제 요원이 신속하게 상황을 파악할 수 있습니다.
3) 데이터 기반 예방 : 감지 이력 · 히트맵 · 반복 패턴 통계를 통해 위험 구간을 사전에 파악하고 예방 조치를 수립할 수 있습니다.
4) AI 관제 지원 : 자연어로 감지 현황 · CCTV 운영 상태를 즉시 조회할 수 있어, 관제 요원의 상황 판단 속도가 향상됩니다.
5) 안정적인 인프라 : Master-Master DB 복제와 Keepalived 자동 페일오버로 단일 서버 장애 상황에서도 서비스 연속성을 확보했습니다.

------------------------------------------------------------------------------------------------------------------------------------------------

18. 향후 프로젝트 확장 방향

1) 단기 계획 : Ollama 기반 로컬 LLM + LangChain 적용으로 오탐률을 줄이고 응답 성능을 향상
2) 중기 계획 : 축적된 탐지 데이터를 활용해 Fine-tuning 기반 자체 모델로 고도화
3) 장기 계획 : 차선 기반 위치 분석을 통한 실시간 위험도 관리로 확장, 예측형 관제 시스템으로 진화
4) 그 외 : 차량 번호판 인식(LPR) 연동, 모바일 관제 앱, 고속도로 VMS 연동 전광판 경고, 엣지 컴퓨팅 배포
